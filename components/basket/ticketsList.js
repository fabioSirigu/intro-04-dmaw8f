//importo la funzione che genera i ticket dentro la list

export const ticketsList = () => {
  const listEl = document.createElement('div');
  listEl.setAttribute('id', 'listBasket');
  return listEl;
};
